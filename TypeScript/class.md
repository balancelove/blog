## 类

```typescript
class Person {
  // 属性方法修饰符：public / private / protected
  public name: string;
  private age: number;
  protected sex: string;
}
// 抽象类和抽象方法，子类必须实现其中的方法
abstract class Animal {
    abstract eat: string() {} 
}
```

